import 'package:flutter/material.dart';

class SwipeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Swipe Screen", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
