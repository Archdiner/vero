import 'package:flutter/material.dart';

class RestaurantDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Restaurant Details Screen", style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
